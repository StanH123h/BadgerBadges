# Backend signer private key (MUST match the address set in deployed contract)
BACKEND_SIGNER_KEY=your_backend_signer_private_key_here

# Contract address (fill after deployment)
NEXT_PUBLIC_ACHIEVEMENTS_CONTRACT_ADDRESS=

# Network to use (localhost, sepolia, mainnet)
NEXT_PUBLIC_NETWORK=localhost

# Optional: Sepolia RPC (if using Sepolia)
NEXT_PUBLIC_SEPOLIA_RPC=https://rpc.sepolia.org

# API keys for external services (TODO: implement these)
# WEATHER_API_KEY=your_openweathermap_key
# GEOCODING_API_KEY=your_geocoding_key
